.main__message-list
  .main__message-list__upper
    %p.main__message-list__upper__talker
      = message.user.name
    %p.main__message-list__upper__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .lower-message
    -if message.content.present?
      %p.main__message-list__text
        =message.content
    = image_tag message.image.url if message.image.present?